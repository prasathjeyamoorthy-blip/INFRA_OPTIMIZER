[pytest]
# Disable langsmith pytest plugin — crashes on Python 3.12 due to pydantic v1 ForwardRef
# incompatibility. Run tests with: PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest
addopts = -p no:langsmith
testpaths = Agent
